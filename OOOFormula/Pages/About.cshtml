@page
@model OOOFormula.Pages.AboutModel
@{
    ViewData["Title"] = Model._page.Title;
}

<div class="text-center mt-5">
    <h1>
        @Html.DisplayFor(model => model._page.Title)
    </h1>
</div>

<div class="row justify-content-center mt-2">
    <div class="col-10">
        @Html.Raw(Model._page.Body)
    </div>
</div>